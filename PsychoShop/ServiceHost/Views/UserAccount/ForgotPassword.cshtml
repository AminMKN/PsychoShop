@model UserAccountCommand
@{
    ViewData["Title"] = "فراموشی کلمه عبور";
    Layout = "_UserAccountLayout";
    if (Model.Message == ApplicationMessages.SentForgotPasswordEmail)
    {
        <script type="text/javascript">
            Swal.fire({
                icon: 'success',
                title: '@Model.Message',
                text: 'فروشگاه اینترنتی سایکو شاپ',
                confirmButtonText: 'باشه',
            })
        </script>
    }else if (Model.Message == ApplicationMessages.RequestedInfoNotFound)
    {
        <script type="text/javascript">
            Swal.fire({
                icon: 'error',
                title: '@Model.Message',
                text: 'فروشگاه اینترنتی سایکو شاپ',
                confirmButtonText: 'باشه',
            })
        </script>
    }
}

<div id="main">
    <div class="col-lg-4 col-md-6 col-xs-12 mx-auto">
        <div class="account-box">
            <a class="logo-account">
                <img class="mt-3" src="~/Theme/assets/images/shoplogo.jpg" alt="ElectronShop" title="ElectronShop">
            </a>
            <span class="account-head-line text-center">@ViewData["Title"]</span>
            <div class="content-account">
                <form method="post">
                    <div asp-validation-summary="ModelOnly"></div>
                    <div>
                        <label asp-for="ForgotPassword.Email" class="pull-right">ایمیل</label>
                        <input asp-for="ForgotPassword.Email" type="email" class="form-control" placeholder="ایمیل">
                        <span asp-validation-for="ForgotPassword.Email" class="text-danger"></span>
                    </div>
                    <div class="parent-btn">
                        <button type="submit" class="dk-btn dk-btn-info">
                            ارسال لینک تغییر کلمه عبور
                            <i class="fa fa-sign-in sign-in"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
}