@model AccountAdminModel
@{
    ViewData["Title"] = "ایجاد کاربر";
    if (Model.Message == ApplicationMessages.TaskSuccessful)
    {
        <script type="text/javascript">
            Swal.fire({
                icon: 'success',
                title: '@Model.Message',
                text: 'فروشگاه اینترنتی سایکو شاپ',
                confirmButtonText: 'باشه',
            })
        </script>
    }
    else if (Model.Message == ApplicationMessages.DuplicatedUserName || Model.Message == ApplicationMessages.DuplicatedEmail)
    {
        <script type="text/javascript">
            Swal.fire({
                icon: 'error',
                title: '@Model.Message',
                text: 'فروشگاه اینترنتی سایکو شاپ',
                confirmButtonText: 'باشه',
            })
        </script>
    }
}

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-12">
            <div class="card shadow mb-4">
                <div class="card-header">
                    <strong class="card-title">@ViewData["Title"]</strong>
                </div>
                <form method="post">
                    <div asp-validation-summary="ModelOnly"></div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-4">
                                <div class="form-group">
                                    <label asp-for="SignUpAccount.FullName">نام و نام خانوادگی</label>
                                    <input asp-for="SignUpAccount.FullName" type="text" class="form-control" placeholder="نام و نام خانوادگی" />
                                    <span asp-validation-for="SignUpAccount.FullName" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="form-group">
                                    <label asp-for="SignUpAccount.UserName">نام کاربری</label>
                                    <input asp-for="SignUpAccount.UserName" type="text" class="form-control" placeholder="نام کاربری" />
                                    <span asp-validation-for="SignUpAccount.UserName" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="form-group">
                                    <label asp-for="SignUpAccount.Email">ایمیل</label>
                                    <input asp-for="SignUpAccount.Email" type="email" class="form-control" placeholder="ایمیل" />
                                    <span asp-validation-for="SignUpAccount.Email" class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-4">
                                <div class="form-group">
                                    <label asp-for="SignUpAccount.MobilePhone">شماره موبایل</label>
                                    <input asp-for="SignUpAccount.MobilePhone" type="tel" class="form-control" placeholder="شماره موبایل" />
                                    <span asp-validation-for="SignUpAccount.MobilePhone" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="form-group">
                                    <label asp-for="SignUpAccount.Password">کلمه عبور</label>
                                    <input asp-for="SignUpAccount.Password" type="password" class="form-control" placeholder="کلمه عبور" />
                                    <span asp-validation-for="SignUpAccount.Password" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="form-group">
                                    <label asp-for="SignUpAccount.RePassword">تکرار کلمه عبور</label>
                                    <input asp-for="SignUpAccount.RePassword" type="password" class="form-control" placeholder="تکرار کلمه عبور" />
                                    <span asp-validation-for="SignUpAccount.RePassword" class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="card-footer">
                        <button type="submit" class="btn btn-success">ایجاد</button>
                        <a class="btn btn-primary" asp-area="Admin" asp-controller="Account" asp-action="Index">بازگشت</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}